public class CountOfOpp {
    public static void OpportunityCount(list<Opportunity> opp)
    {
        set<id> ids=new set<id>();
    for(opportunity op:opp)
    {
        ids.add(op.id);
    }
        
    }
}